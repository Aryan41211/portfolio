# Bug Fix Todo ✅ ALL DONE

## Fixes
- [x] Fix 1: Section.tsx — add missing Container import ✅
- [x] Fix 2: Button.tsx — convert <button> to <motion.button> + proper HTMLMotionProps type ✅
- [x] Fix 3: Badge.tsx — convert <a> to <motion.a> in IconButton + proper HTMLMotionProps type ✅
- [x] Fix 4: Reveal.tsx — add missing React import (used in StaggeredReveal) ✅
- [x] Fix 5: 6 section files — fix SectionHeader prop mismatch (sectionId → id) ✅
- [x] Validate: Build succeeds ✅

